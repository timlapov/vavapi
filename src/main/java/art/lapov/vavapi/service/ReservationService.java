package art.lapov.vavapi.service;

import org.springframework.stereotype.Service;

import java.time.LocalDateTime;

@Service
class ReservationService {

    // Creates a new reservation
//    ReservationDto createReservation(String userId, String stationId, LocalDateTime start, LocalDateTime end);
//
//    // Cancels a reservation
//    void cancelReservation(String reservationId, String userId);
//
//    // Returns all reservations of a user
//    List<ReservationDto> getUserReservations(String userId);
//
//    // Returns a reservation by its ID
//    ReservationDto getReservationById(String reservationId);
//
//    // Leaves a review for a completed reservation
//    ReviewDto leaveReview(String reservationId, String userId, String text, int rating);

}
